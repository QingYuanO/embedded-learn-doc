---
globs: content/docs/hal/**/*.mdx
alwaysApply: false
---
# STM32 HAL库函数文档

## 文档结构规范

每个 STM32 HAL 库外设文档必须按照以下结构组织：

### 1. 文档头部信息

```yaml
---
title: stm32f1xx_hal_[外设名称]
description: STM32F1xx [外设名称] 外设 HAL 库函数参考文档
---
```

### 2. 完整文档结构

1. **外设概述**
   - 外设简介
   - 主要特性

2. **数据类型定义**
   - 枚举类型
   - 结构体类型
   - 宏定义

3. **HAL 库函数详解**
   - 初始化函数
   - 控制函数
   - 状态查询函数
   - 中断处理函数

4. **使用示例**

5. **注意事项**

6. **总结**

## 各部分详细规范

### 外设概述部分

```md
## 外设概述

### 简介
[简要介绍外设的功能和用途]

### 主要特性
- 特性1：详细说明
- 特性2：详细说明
- 特性3：详细说明
```

### 数据类型定义部分

#### 枚举类型格式：
```md
#### [枚举名称] - [中文描述]

\```c
typedef enum
{
  枚举值1 = 0x值,    /* 中文说明 */
  枚举值2 = 0x值     /* 中文说明 */
} 枚举类型名称;
\```

**说明：**
- `枚举值1`：详细功能说明
- `枚举值2`：详细功能说明
```

#### 结构体类型格式：
```md
#### [结构体名称] - [中文描述]

\```c
typedef struct
{
  数据类型 成员1;    /* 成员说明 */
  数据类型 成员2;    /* 成员说明 */
} 结构体类型名称;
\```

**成员说明：**
- `成员1`：详细说明成员用途和取值范围
- `成员2`：详细说明成员用途和取值范围
```

#### 宏定义格式：
```md
#### [宏定义名称] - [中文描述]

\```c
#define 宏名称  宏值    /* 宏定义说明 */
\```

**说明：**
[详细说明宏定义的用途和含义]
```

### HAL 库函数详解部分

**函数分类组织：**

1. **初始化函数**：`HAL_[外设]_Init`、`HAL_[外设]_DeInit`、`HAL_[外设]_MspInit` 等
2. **控制函数**：启动、停止、配置等操作函数
3. **状态查询函数**：获取外设状态的函数
4. **中断处理函数**：中断回调函数
5. **其他函数**：不属于上述分类的函数

**每个函数必须包含以下内容：**

1. **函数标题**：三级标题 `###` + 序号 + 函数名
2. **函数代码块**：包含完整的 Doxygen 注释和函数声明
3. **功能说明段落**：详细说明函数的功能、参数和返回值
4. **使用示例**：简单的使用示例代码

#### 函数格式模板：

```md
### [序号]. [函数名]

\```c
/**
 * @brief  [函数功能的简要描述]
 * @param  [参数名]: [参数详细说明，包括可选值]
 * @param  [参数名]: [参数详细说明，包括可选值]
 * @retval [返回值说明，无返回值写"无"]
 * @note   [重要注意事项]
 * @example
 *     [典型使用示例，缩进四个空格]
 *     [可以多行，每行都要缩进]
 */
返回类型 函数名(参数列表);
\```

**功能说明：**
[详细描述函数的功能、用途和注意事项]

**参数说明：**
- `参数1`：详细说明参数的含义、取值范围和注意事项
- `参数2`：详细说明参数的含义、取值范围和注意事项

**返回值说明：**
- `返回值1`：详细说明返回值的含义
- `返回值2`：详细说明返回值的含义

**使用示例：**
\```c
[简单的使用示例代码]
\```
```

### 使用示例部分

```md
## 使用示例

### [示例标题]

**功能描述：**
[说明示例要实现的功能]

**硬件连接：**
[说明硬件连接方式]

**代码实现：**
\```c
[完整的可运行代码示例]
\```

**运行结果：**
[说明运行后的预期结果]
```

### 注意事项部分

```md
## 注意事项

1. **[注意点类别]**：具体说明
2. **[注意点类别]**：具体说明
3. **[注意点类别]**：具体说明
```

### 总结部分

```md
## 总结

[对整个外设 HAL 库的总结，强调重要功能和使用要点]
```

## 编写要求

### 内容要求
1. **中文描述**：所有说明文字使用中文，简明扼要
2. **完整性**：涵盖外设的所有主要 HAL 库函数
3. **实用性**：示例代码能够直接复制使用
4. **准确性**：技术描述准确，参数说明详细
5. **可读性**：函数说明要通俗易懂，避免过于技术化的描述

### 格式要求
1. **一致性**：所有文档使用相同的格式和结构
2. **可读性**：合理使用标题层级和代码块
3. **规范性**：严格按照模板格式编写
4. **层次性**：函数按功能分类组织，便于查找

### 示例代码要求
1. **典型用法**：展示最常见的使用场景
2. **完整性**：包含必要的初始化和配置步骤
3. **可执行性**：代码应该能够直接编译运行
4. **渐进性**：从简单到复杂，逐步展示功能
5. **注释性**：代码要有详细的中文注释

### HAL 库特定要求
1. **函数分类**：按照 HAL 库的函数类型进行分类
2. **错误处理**：说明 HAL 库的错误处理机制
3. **状态管理**：说明 HAL 库的状态管理方式
4. **中断处理**：详细说明中断回调函数的使用

## 质量检查清单

- [ ] 文档结构完整，包含所有必要部分
- [ ] 所有函数都有完整的 Doxygen 注释
- [ ] 示例代码可以直接运行
- [ ] 中文描述准确易懂
- [ ] 格式统一规范
- [ ] 技术内容无误
- [ ] 函数按功能分类组织
- [ ] 包含 HAL 库特有的错误处理和状态管理说明
- [ ] 中断回调函数说明完整
- [ ] 代码示例有详细注释
- [ ] 示例代码可以直接运行
- [ ] 中文描述准确易懂
- [ ] 格式统一规范
- [ ] 技术内容无误