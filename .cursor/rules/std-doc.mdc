---
alwaysApply: true
---

# 标准库函数

## 规则说明

1. **每个标准库函数的文档示例必须包含以下内容：**

   - 函数名称（以三级标题 `###` 开头，序号自动递增）
   - C 语言函数声明代码块，包含 Doxygen 风格注释，注释内容包括：
     - `@brief`：简要描述函数功能
     - `@param`：详细列出所有参数及其含义
     - `@retval`：返回值说明（如无返回值，写“无”）
     - `@example`：给出典型调用示例，示例代码缩进四个空格
   - 最后一行为函数声明

2. **注释内容要求：**

   - 中文描述，简明扼要，便于初学者理解
   - 示例代码应为常见用法，能直接复制运行

3. **格式示例：**

````md
### 1.TIM_DeInit
```c
/**
 * @brief  将定时器外设寄存器重设为默认值
 * @param  TIMx: 选择要重置的定时器外设
 * @retval 无
 * @example
 *     TIM_DeInit(TIM2);
 */
void TIM_DeInit(TIM_TypeDef* TIMx);
```
````
