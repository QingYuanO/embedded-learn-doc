---
globs: content/docs/std/**/*.mdx
alwaysApply: false
---
# STM32标准库函数文档

## 文档结构规范

每个 STM32 标准库外设文档必须按照以下结构组织：

### 1. 文档头部信息

```yaml
---
title: stm32f10x_[外设名称]
description: STM32F10x [外设名称] 外设标准库函数参考文档
---
```

### 2. 完整文档结构

1. **数据类型定义**
   - 枚举类型
   - 结构体类型

2. **寄存器定义**（如适用）

3. **中断标志定义**（如适用）

4. **事件定义**（如适用）

5. **标准库函数详解**

6. **使用示例**

7. **注意事项**

8. **总结**

## 各部分详细规范

### 数据类型定义部分

#### 枚举类型格式：
```md
#### [枚举名称] - [中文描述]

\```c
typedef enum
{
  枚举值1 = 0x值,    /* 中文说明 */
  枚举值2 = 0x值     /* 中文说明 */
} 枚举类型名称;
\```

**说明：**
- `枚举值1`：详细功能说明
- `枚举值2`：详细功能说明
```

#### 结构体类型格式：
```md
#### [结构体名称] - [中文描述]

\```c
typedef struct
{
  数据类型 成员1;    /* 成员说明 */
  数据类型 成员2;    /* 成员说明 */
} 结构体类型名称;
\```

**成员说明：**
- `成员1`：详细说明成员用途和取值范围
- `成员2`：详细说明成员用途和取值范围
```

### 寄存器定义部分

```md
## [外设名称]寄存器定义

STM32F10x [外设名称]外设包含以下寄存器：

\```c
#define 外设_Register_寄存器名1  ((uint8_t)0x偏移)  /* 寄存器中文名称 */
#define 外设_Register_寄存器名2  ((uint8_t)0x偏移)  /* 寄存器中文名称 */
\```
```

### 中断标志定义部分

```md
## [外设名称]中断标志定义

### 基本中断类型

\```c
#define 外设_IT_类型1  ((uint16_t)0x值)  /* 中断类型说明 */
\```

### 具体中断标志

\```c
#define 外设_IT_标志名1  ((uint32_t)0x值)  /* 中断标志说明 */
\```
```

### 标准库函数详解部分

**每个函数必须包含以下内容：**

1. **函数标题**：三级标题 `###` + 序号 + 函数名
2. **函数代码块**：包含完整的 Doxygen 注释和函数声明
3. **功能说明段落**：简要说明函数的主要功能

#### 函数格式模板：

```md
### [序号]. [函数名]

\```c
/**
 * @brief  [函数功能的简要描述]
 * @param  参数名: [参数详细说明，包括可选值]
 * @param  参数名: [参数详细说明，包括可选值]
 * @retval [返回值说明，无返回值写"无"]
 * @example
 *     [典型使用示例，缩进四个空格]
 *     [可以多行，每行都要缩进]
 */
返回类型 函数名(参数列表);
\```

**功能说明：**
[详细描述函数的功能、用途和注意事项]
```

### 使用示例部分

```md
## 使用示例

### [示例标题]

\```c
[完整的可运行代码示例]
\```
```

### 注意事项部分

```md
## 注意事项

1. **[注意点类别]**：具体说明
2. **[注意点类别]**：具体说明
```

### 总结部分

```md
## 总结

[对整个外设库的总结，强调重要功能和使用要点]
```

## 编写要求

### 内容要求
1. **中文描述**：所有说明文字使用中文，简明扼要
2. **完整性**：涵盖外设的所有主要功能和API
3. **实用性**：示例代码能够直接复制使用
4. **准确性**：技术描述准确，参数说明详细

### 格式要求
1. **一致性**：所有文档使用相同的格式和结构
2. **可读性**：合理使用标题层级和代码块
3. **规范性**：严格按照模板格式编写

### 示例代码要求
1. **典型用法**：展示最常见的使用场景
2. **完整性**：包含必要的初始化和配置步骤
3. **可执行性**：代码应该能够直接编译运行
4. **渐进性**：从简单到复杂，逐步展示功能

## 质量检查清单

- [ ] 文档结构完整，包含所有必要部分
- [ ] 所有函数都有完整的 Doxygen 注释
- [ ] 示例代码可以直接运行
- [ ] 中文描述准确易懂
- [ ] 格式统一规范
- [ ] 技术内容无误